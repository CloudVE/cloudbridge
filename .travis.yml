language: python
python: 3.6
os:
  - linux
#  - osx
env:
  - TOX_ENV=py27
  - TOX_ENV=py36
#  - TOX_ENV=pypy
matrix:
  fast_finish: true
  allow_failures:
    - os: osx
install:
  - pip install tox
  - pip install coveralls
  - pip install codecov
script:
  - tox -e $TOX_ENV
after_success:
#  - coveralls
  - codecov --token=1066c661-d8df-487f-9c22-bf20ee2975d3
